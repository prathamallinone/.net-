using System.Diagnostics.CodeAnalysis;
using System.Security.Cryptography.X509Certificates;

namespace tata
{
    public class Baike
    {
        public static void Main()
        {
            Baike.Part();

        }


        public static void Part(string leg = "pi.1")
        {
            try
            {
               char a =Convert.ToChar(leg);
            }
            catch (InvalidProgramException ex)
            {
                Console.WriteLine(leg);
            }

            catch (Exception ex) 
            {
                Console.WriteLine("youre woring input ");
            }
            finally
            {
                Console.WriteLine("aub aata hai aap ko ky");
            }
        }

    }
}